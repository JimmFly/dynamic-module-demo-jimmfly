/**
 * Egg module with various egg-related functions
 */

/**
 * Make an egg lay
 * @param name - The egg's name
 * @returns Laying message
 */
export const lay = (name: string): string => {
  return `Egg ${name} is laying`;
};
